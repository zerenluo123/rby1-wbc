# Teleop configuration for iPhone ARKit control.
host: "0.0.0.0"   # Address to bind the local pose server
port: 5555        # TCP port for the iPhone app to connect
portrait: true    # Apply portrait-mode rotation to incoming poses
